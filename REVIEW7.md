# Seventh production-risk review — everything after REVIEW6, including REVIEW6's own remediation

Unlike the six reviews before it, this one did not stop at findings: the operator asked for every
confirmed finding fixed, so this document records each finding **and what was done about it**.
Fixes landed as commit `4d610886` (this review's batch), on top of `1eb275a2` (the REVIEW6
leftover items) and `27c97109` (checklist/docs corrections). The review itself covered
`5d1416a4..ea78522e` — the 26 commits nobody had reviewed since REVIEW6 — with six parallel
read-only sub-reviews, one per risk cluster:

| Sub-review | Cluster                                                               | Commits examined                                           |
| ---------- | --------------------------------------------------------------------- | ---------------------------------------------------------- |
| A          | Dashboard nightly-snapshot rework + field-operations card             | `7cdbd687`, `b0864f46`                                     |
| B          | Adversarial re-read of the remediation commits themselves             | `d28a68af`, `1eb275a2`                                     |
| C          | Demo workspaces gating as the Movement plan + demo seed data          | `3b2b00c4`, `a0572b49`, `3f6d15fe`, `c86d58db`             |
| D          | Command-palette global search, cross-record links, detail-page rework | `b87635bd`, `e6d78287`, `7a696b30`, `ea78522e`, `a37c524b` |
| E          | Canvassing walk-sheet cluster                                         | `a0bded3f`, `64e372e7`, `739833e7`, `0173ae4c`, `50b782e3` |
| F          | Marketing-website claims + monitoring probe reduction                 | `f2b9118d`, `fe469a18`, `438839fb`, `04772277`, `4dc36161` |

Each sub-review returned findings with file:line evidence plus a verified-clean list; the
orchestrator re-read the load-bearing claims at the cited lines while writing the fixes, which
serves as the verification pass. Verification of the fix batch: root eslint on every changed
file, `nx lint` on all five projects, production builds of backend/frontend/companion/website,
and full test suites for common, companion, frontend and backend — all green at `4d610886`.

Status legend: **FIXED** (in `4d610886` unless noted) · **LEFT — deliberate** (with the reason)
· **OPERATOR** (needs a dashboard/portal, not code).

---

## First, the status of REVIEW6's own leftovers (fixed before this review ran)

Commit `1eb275a2` closed the items REVIEW6 left open: total-request timeouts on the Gmail send,
both Microsoft Graph clients, the Gmail sync fetch helper and the SendGrid domain-setup call
(T2-3); the boundary freshness stamp writes only when every requested layer loaded (T2-19);
Sentry capture when a Stripe cancel fails during a tenant wipe (T2-11); the seat-quantity clamp
in the subscription webhook (T2-12 — later corrected by this review's B1, below); an honest
`rejected` ack for cross-campaign yard-sign handovers (T2-15); the companion offline queue slices
to the server's 200-op cap (T2-16a); cloned teams keep members outside the picker window (T2-13);
undo cannot restore a delivery stop onto a canceled route (T2-14); and an autovacuum tuning
migration for the four churn tables (T2-8). Commit `27c97109` closed the seven documentation
gaps (passkey relying-party variable, annual price IDs, boundary-map upload step, corrected
GitHub-secrets list, beta-approval hold in the smoke test, TRUST_PROXY measurement item, stale
"catalog is empty" comment).

Still open from REVIEW6, unchanged in kind: the operator actions in its tables A/B (live Stripe
configuration, mail-provider go-live, map upload, passkey secret, and the rest), REVIEW6 T2-16b/c
and T2-18 (small canvassing/import items, accepted), the T2-1 remainder (synchronous JSON.parse
on the request path — reduced in impact by this review's B2 fix), and the Container-App-in-bicep
hardening.

---

## Sub-review A — dashboard snapshot rework and field-operations card

| #   | Finding                                                                                                                                                                                                              | Status                                                                                                                                                                                                                          |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1  | The dashboard still loaded **every open inbox email** into Node per page view just to produce three integers ([dashboard/controller.ts](apps/backend/src/app/modules/dashboard/controller.ts)).                      | **FIXED** — one `GROUP BY assigned_to` aggregate with `min(created_at)`; no rows ship to Node.                                                                                                                                  |
| A2  | Both SLA-breach candidate queries were unbounded, and their pagination re-ran the full computation per page.                                                                                                         | **FIXED** — both capped at 2,000 candidates, oldest first (`BREACH_CANDIDATE_CAP`), so the longest-breached rows are the ones kept; past the cap the count reads "at least". The per-page recompute remains but is now bounded. |
| A3  | The nightly stats sweep ran every workspace in one job under the default 15-minute timeout, and a mid-sweep death left the cron chain unseeded until the next boot.                                                  | **FIXED** — long timeout override; `scheduleNextRun` moved into a `finally`; per-tenant try/catch was already correct.                                                                                                          |
| A4  | Field-ops counts included `'cleared'` undo markers and counted knock rows rather than doors — the dashboard disagreed with the canvassing page.                                                                      | **FIXED** — `'cleared'` excluded everywhere; conversations count distinct households.                                                                                                                                           |
| A5  | No campaign scoping on the knock counts: a campaign-pinned Editor saw the whole workspace's numbers.                                                                                                                 | **FIXED** — join to `turfs` with the campaign pin applied. The two readiness counts (sign requests, waiting volunteers) predate the card and stay tenant-wide — they are queue totals, not campaign metrics.                    |
| A6  | The card's gating didn't match the sidebar (deliveries row gated on the **canvassing** feature key; volunteers row ungated).                                                                                         | **FIXED** — each row gates on its own feature key (`canvassing`/`deliveries`/`companions`).                                                                                                                                     |
| A7  | The readiness counts could permanently skip on a cold load (user signal arriving after `ngOnInit`).                                                                                                                  | **FIXED** — effect-driven, once-guarded load.                                                                                                                                                                                   |
| A8  | While a refresh was pending the page re-ran the **full** dashboard read every 4 seconds for up to a minute.                                                                                                          | **FIXED** — new single-row `dashboard.getSnapshotStatus` query is polled instead; the full read happens once, after the snapshot lands.                                                                                         |
| A9  | The read-path bootstrap enqueue had no rate limit (idempotent, but simultaneous first visitors queued duplicate jobs).                                                                                               | **FIXED** — same limiter as the Refresh button; hitting it skips the enqueue rather than failing the read.                                                                                                                      |
| A10 | Neither the live reads nor the snapshot job excluded `detached_at` mail — the dashboard counted messages the inbox hides, and a detached email could sit in the breach list forever. Pre-existing, not a regression. | **FIXED** — `detached_at IS NULL` on the open-email read, both breach queries, and both snapshot aggregate passes.                                                                                                              |
| A11 | The nightly chain drifted forward by each night's runtime; enough drift skips a UTC calendar date in the snapshot history.                                                                                           | **FIXED** — the sweep anchors its successor to the next 02:30 UTC.                                                                                                                                                              |
| A12 | The sweep included pending-approval and deletion-scheduled workspaces. Cost only.                                                                                                                                    | **FIXED** — filtered to approved, not-scheduled-for-deletion tenants.                                                                                                                                                           |

Sub-review A verified clean (kept for the record): tenant filtering in every new query including
the raw-SQL laterals; the tenant lint rule now covers `lib/**`; 400-day snapshot retention;
`(tenant_id, snapshot_date)` upsert idempotency; advisory-locked rescheduling; snapshots in the
tenant-wipe list with CASCADE and forced RLS; the no-snapshot-yet UI state; the versioned payload
schema; rolling windows computed in the database (no timezone bug); index support for the new
queries; and the tasks-repo SLA cutoff change.

## Sub-review B — the remediation commits, read adversarially

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                      | Status                                                                                                                                                                                                                        |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1  | **The webhook seat clamp introduced by `1eb275a2` (REVIEW6 T2-12) had a money bug**: with an unrecognized price ID (e.g. a missing annual env var), the plan fell back to the stored one — worst case `free`, ladder max 1 — and the clamp then **pushed quantity 1 to the paying customer's Stripe subscription** with no proration. The billing-page reconciler had the same latent shape. | **FIXED** — the clamp (and its push) run only when the price was recognized; an unknown price degrades to "store the raw value" plus a warning naming the env vars.                                                           |
| B2  | `d28a68af`'s cache changes made any published map file above ~8 MB **permanently uncacheable** (48 MB budget ÷ raw-bytes×6 sizing; the LRU evicted its own newest entry) — the nightly match pass re-downloaded, re-checksummed and re-parsed the file once per 500-household page.                                                                                                          | **FIXED** — the cache never evicts the entry just inserted (bounded overshoot beats the re-parse loop), and published files are sized at raw-bytes×3, since raw GeoJSON carries properties the parser discards.               |
| B3  | The three new retention prunes (`workflow_runs`, `notifications`, `companion_ops`) filtered on `created_at` with **no usable index** — repeated sequential scans nightly, in the same commit whose own migration documented that exact failure class.                                                                                                                                        | **FIXED** — migration `2026-08-14-retention-prune-indexes.ts` adds the three `created_at` b-trees. The prune WHERE clauses themselves were verified correct against every reader.                                             |
| B4  | The freshness-stamp guard (this session's T2-19 fix) turns a permanently unreadable map file into an **unbounded nightly re-scan with no alert** — and a never-uploaded blob file is an expected operator mistake.                                                                                                                                                                           | **FIXED** — one Sentry capture per pass (first page only) naming the tenant and layer counts. The re-scan itself remains, by design: it is what retries once the file appears.                                                |
| B5  | The send-path timeouts added in `1eb275a2` made a **timed-out send read as a clean failure**: the Microsoft catch deleted the local email row (inviting a duplicate send of a message Graph may have delivered), and 60 s was tight for Gmail's single ~33 MB request.                                                                                                                       | **FIXED** — a deadline abort now keeps the message in Drafts with an "it may still have gone through — check Sent" message on both providers; the budget is 120 s.                                                            |
| B6  | The survey path still silently discarded a cross-campaign sign handover — the exact defect just fixed for the standalone op, minus the ability to reject (the survey's conversation/DNC data must land).                                                                                                                                                                                     | **FIXED** — the ack gained an optional `warning` field: the survey applies, the volunteer sees "the sign handover was not recorded — tell your organizer", the warning is persisted for retries, and the phone shows it once. |
| B7  | The automations last-run `DISTINCT ON` still walked every run row (no skip-scan below Postgres 18); only the JS cost had been removed.                                                                                                                                                                                                                                                       | **FIXED** — 90-day bound matching the retention window, so it can only hide rows the sweep is about to delete.                                                                                                                |
| B8  | The Gmail retry helper's `init?.signal ?? timeout` would silently share one caller-supplied signal across all retries — latent, contradicting its own comment.                                                                                                                                                                                                                               | **FIXED** — `AbortSignal.any` combines the caller's signal with a fresh per-attempt deadline.                                                                                                                                 |
| B9  | The pre-send gate compared verified domains without the normalization the shared From-address rule uses (safe direction, but "one rule everywhere" was untrue).                                                                                                                                                                                                                              | **FIXED** — same lowercase/trim on both sides.                                                                                                                                                                                |

Sub-review B verified clean: the From-address policy is consistent at all four decision points
with Reply-To correctly outside DKIM alignment; the boundary diff-before-write handles a
household leaving all areas and stays tenant/set-scoped; the phone routes `rejected` acks to its
held list with no auto-retry and the rolled-back ledger row makes a manual retry clean;
cross-campaign detection cannot misfire for the same campaign; undo's canceled-route path writes
the same states route-cancel does; the seat clamp cannot oscillate with the webhook; the
Graph/SendGrid timeout signals are per-request; the autovacuum migration is valid; and the
duplicates change gate, boot guard and settle-delay coalescing all hold.

## Sub-review C — demo workspaces gating as Movement

| #   | Finding                                                                                                                                                                                                                                                                                                                                       | Status                                                                                                                                                                                                                                                                                                                                |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | **Text messages had no tenant gate at all**: the demo elevation opened the Movement-only volunteer flows, whose turf assignments and one-time codes send real Twilio texts to audience-chosen numbers — from unpaid, anonymous workspaces, on the platform's account. Rate-metered but real spend and real messages.                          | **FIXED** — the SMS enqueue path checks the tenant: suspended workspaces send nothing; unpaid demo workspaces send nothing (skip-and-log, so the business write it rides in still lands). Latent in production only if Twilio credentials are unset — not verifiable from the repo.                                                   |
| C2  | **The scriptable write API was opened to unpaid workspaces and its data survives the demo**: mint a key during the demo, bulk-load a purchased list through the Zapier/upsert endpoints, exit to Free with the list intact — precisely the abuse the paid gate exists to prevent, per the plan file's own comment.                            | **FIXED** — `DEMO_EXCLUDED_FEATURES` in the shared plans library: the `api` feature always gates on the stored plan; the key validator and the generic plan gate both honor it.                                                                                                                                                       |
| C3  | **A paying customer who had not yet removed the demo data silently lost all contact mail — including donation tax receipts** (recorded as issued, never emailed, no signal) — and their drip automations stalled hourly. "Paid, demo present" is the normal transitional state, since exiting the demo requires a settled subscription first. | **FIXED** — the demo block on contact transactional mail and the drip deferral both exempt paid stored plans. An unpaid demo workspace still sends nothing ("nothing you do in the demo reaches a real person" holds where it matters — unpaid demo workspaces cannot take donations anyway, since Stripe Connect refuses demo mode). |
| C4  | The demo seed's ~200 phantom emailable subscribers counted toward the billing meter — able to push a real customer over a bracket boundary and trigger an immediate prorated Stripe charge.                                                                                                                                                   | **FIXED** — the bracket notify-and-push block is skipped entirely while demo data is present; the first usage check after the exit reconciles against the real count.                                                                                                                                                                 |
| C5  | Seeded demo phone numbers sat outside 555-0100–0199, the only block reserved for fiction — the generator emitted 0300+, and (found while fixing) the hand-written datasets also carried 0201–0268, 0310–0315 and 0410–0414. Combined with C1 these were potentially assignable numbers.                                                       | **FIXED** — generator constrained to 555-01XX; all 31 out-of-range hand-written numbers remapped into free 01XX slots; two spec assertions now pin the range for every dataset and every place pack.                                                                                                                                  |
| C6  | Demo detection is the `demo_mode_at` flag alone — hand-deleting the seeded rows leaves the workspace in demo state with no explanation.                                                                                                                                                                                                       | **LEFT — deliberate.** The C3 paid exemption removes the harmful consequence; for unpaid workspaces the demo card explains the state, and the exit flow is one click.                                                                                                                                                                 |
| C7  | Real geocoding spend for unpaid demo workspaces.                                                                                                                                                                                                                                                                                              | **LEFT — pre-existing and unchanged** by the gating commit (verified a pure refactor); bounded by the Free import cap and the daily geocode budget.                                                                                                                                                                                   |

Sub-review C verified clean: newsletter send/resend/test all refuse demo server-side; the
goodbye email rides the deferred drip worker; invites, mailbox connects and Stripe Connect all
`assertNotDemoMode`; the effective-plan helper never writes billing state; the inbox purge is
behaviorally unchanged; the seed manifest covers every written table with cascade-safe deletes;
no seeded row escapes the tenant; every seeded email address is on `example.*`/`pplcrm.example`.

## Sub-review D — global search, cross-links, detail pages

| #   | Finding                                                                                                                                                                                                                                                                                       | Status                                                                                                                                                                                                   |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | The palette sent `limit: 4`, which the paging helper **ignores** — each keystroke's two searches returned up to `MAX_PAGE_SIZE` (5,000) full grid rows plus an unbounded count, with no minimum query length.                                                                                 | **FIXED** — the palette sends a real `startRow`/`endRow` window of 4 and searches only from 2 characters.                                                                                                |
| D2  | **A Viewer could open the workspace-wide audit log**: the sidebar's ADMIN group carries `adminOnly` on the parent only, the palette flattened children unflagged, and `/activity` had no route guard at all. The Approvals entry had the inverse gap (guarded route, unflagged sidebar item). | **FIXED** — the flatten inherits the parent's flag; `/activity` is role-guarded like the group that advertises it; the Approvals sidebar item is flagged `adminOnly`.                                    |
| D3  | The "dead `/shifts` link" fix had replaced it with `/events` — also dead (no index child).                                                                                                                                                                                                    | **FIXED** — `/events/shifts`.                                                                                                                                                                            |
| D4  | **Ctrl+K stopped working for the rest of the session after visiting any grid page**: grids counted themselves in `ngOnInit`/`ngOnDestroy`, but the route-reuse strategy detaches grid pages without ever running `ngOnDestroy`.                                                               | **FIXED** — presence is tracked by element, and the ask-time check tests `isConnected`, which is false for a detached component.                                                                         |
| D5  | New cross-links pointed non-admins at role-guarded routes (`/workspace/campaigns/:id` from turf pages and person campaign history; the volunteers dashboard tile) — a click silently bounced to the dashboard.                                                                                | **FIXED** — campaign names render as plain text for non-privileged roles; the volunteers tile requires a privileged role (its count endpoint was always fine).                                           |
| D6  | The palette's address search is unindexable (leading-wildcard over a concatenation).                                                                                                                                                                                                          | **LEFT — deliberate.** Pre-existing: the surrounding OR-list already forced a sequential scan. D1's row cap and minimum length bound the damage; an expression index would still not rescue the OR-list. |
| D7  | Module/plan gating rules are re-implemented three times (sidebar dims, palette drops, dashboard card requires 'on') and drift.                                                                                                                                                                | **LEFT — deliberate.** The divergence is in the safe direction everywhere (the palette hides more than the sidebar); consolidation is refactoring debt, recorded here rather than rushed.                |
| D8  | Workspace-settings deep links ignored the org-mode filter (a church admin saw "Workspace settings → Campaigns").                                                                                                                                                                              | **FIXED** — same electoral-mode rule as the settings page's own nav.                                                                                                                                     |

Also fixed: the keyboard-shortcuts help article's Ctrl+K row now mentions the palette fallback on
pages with no list. Sub-review D verified clean: tenant scoping on both new search predicates,
placeholder-household exclusion, no campaign leak (the rolodex is deliberately tenant-wide), the
other five cross-links, the detail-page rework's activity-log placement, and the per-OS key
labels being display-only.

## Sub-review E — the walk-sheet cluster

| #   | Finding                                                                                                                                                                                                                                                    | Status                                                                                                                                                                                    |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1  | **Opening the print view silently minted a permanent, unlimited-use volunteer join credential** (no expiry, no use cap) — printed as a QR and 8 typeable characters on paper. Mitigated only by the phone-verify + admin-approval steps behind redemption. | **FIXED** — auto-minted print codes expire in 30 days; reprinting after expiry mints a fresh one. The mint-on-view itself is accepted: the code is now a bounded, turf-scoped credential. |
| E2  | The walk sheet's street basemap needs the **Maps Static API**, which the go-live checklist's browser-key restrictions excluded — production configured per checklist would silently print schematic-only sheets forever.                                   | **FIXED** (docs) — GO-LIVE §6 names the API restriction and the separate project-level enablement.                                                                                        |
| E3  | "Door 3 on paper" was not door 3 on the phone: paper numbered the whole turf per-unit; the phone numbers per street and folds a building's units into one row — a 40-unit building offset everything after it by 39.                                       | **FIXED** — paper now numbers per street and gives a building's units one shared number, matching the phone; the spec pins the new numbering.                                             |
| E4  | The auto-minted code was filed under the **currently selected campaign**, not the turf's — duplicate live codes per turf, and redeeming volunteers attached to a campaign that does not own the turf.                                                      | **FIXED** — the client passes the turf's campaign explicitly, and the server now derives the code's campaign from the turf itself (also validating turf ownership).                       |
| E5  | With exactly one door left to walk, the route line was drawn through every already-walked door.                                                                                                                                                            | **FIXED** — a lone remaining door gets no line; a fully finished turf keeps its deliberate walked-shape rendering.                                                                        |
| E6  | Two overlapping same-map areas render differently in the surviving aggregate column after the seat-column drop.                                                                                                                                            | **LEFT — deliberate.** Requires overlapping uploaded polygons; the value is still visible, just joined.                                                                                   |
| E7  | The Static Maps `<img>` relies on the default referrer policy; a future site-wide `no-referrer` header would 403 every basemap silently.                                                                                                                   | **LEFT — noted as a trap** for whoever adds such a header; correct as written today.                                                                                                      |

Sub-review E verified clean: one shared walking-order implementation with deterministic
tiebreakers and fully-determined server ordering; the Mercator overlay matches the requested
image; the QR fetch genuinely cannot toast; the QR encodes a server-built join URL, never a
session token; the backend changes are presentational with tenant scoping intact; both grids got
the identical seat-column change with tests.

## Sub-review F — website claims and monitoring

| #   | Finding                                                                                                                                                                                                                                                    | Status                                                                                                                                                                                                                                           |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F1  | The federal use-case page promised turfs bounded by **polling-division lines**; no polling-division map ships, and imported poll columns carry no polygons. The provincial page stated the same mechanic correctly.                                        | **FIXED** — both federal sentences now use the finest-boundary wording ("the polling division if you have uploaded one, the riding line from the built-in map otherwise").                                                                       |
| F2  | Four pages now advertise the one-click published maps — a flow that fails everywhere until the map files are uploaded to production storage.                                                                                                               | **LEFT — launch sequencing**, already the GO-LIVE §8 upload step (added in `27c97109`); the copy is true the moment the upload runs. This finding raises that step's stakes, which the checklist entry states.                                   |
| F3  | The probe-cost commit removed the `app`/`go` webtests from the Bicep template, but the workflow deploys **Incrementally** — Azure never deletes them, so the orphans are most likely still billing at the old frequency while no repo file describes them. | **FIXED** (docs) + **OPERATOR** — GO-LIVE §10 now instructs listing the availability tests in the portal and either deleting the two orphans or re-adding them to the template, and recording which world was found. Only the operator can look. |
| F4  | The worker probe's PT30M alert window adds ~10 minutes to worst-case time-to-page (~31 → ~41 min) and introduces a new false-page mode: two unrelated single-location blips 25 minutes apart now satisfy the 2-location criterion.                         | **LEFT — recorded decision.** The threshold arithmetic itself was verified sound; the cost trade was deliberate. This paragraph is the record the reviewer asked for.                                                                            |
| F5  | Dropping the two US probe agents means a West-Coast-only partial outage has no watcher.                                                                                                                                                                    | **LEFT — recorded decision**, same as F4.                                                                                                                                                                                                        |
| F6  | The security page's monitoring sentence was not revisited when two probes were switched off and one slowed — the claims registry has a standing rule for exactly that sentence.                                                                            | **FIXED** — sentence reworded to what is true (API probes every few minutes; background processing on a slower cycle), `updated:` bumped.                                                                                                        |
| F7  | All three competitor charts shared one `CHECKED_ON` constant with no staleness mechanism — editing one chart re-dated 43 cells.                                                                                                                            | **FIXED** — per-chart dates with a comment stating the rule. (No build-time staleness check: the website has no test infrastructure; noted as future work.)                                                                                      |
| F8  | The phone layout collapsed the vendor citation link with the prose — three bare "Not offered" verdicts against named companies with no visible evidence, which is the screenshot a vendor's lawyer takes.                                                  | **FIXED** — the source link renders outside the collapsed region; only the explanation collapses.                                                                                                                                                |
| F9  | The claims registry's platform-sending-domain sweep list was missing the three new "your own verified domain" instances.                                                                                                                                   | **FIXED** — registry skill updated with all three locations.                                                                                                                                                                                     |
| F10 | "Every message gets an owner, a due date and a service-level clock" — the due date is a manual create-task step, not automatic.                                                                                                                            | **FIXED** — both instances now say "one click turns it into a task with a due date".                                                                                                                                                             |

Sub-review F verified clean: no numeric import limits anywhere in the new pages (operator
directive holds); no image assets (inline SVG only); sitemap/llms.txt updated; the compile-time
source-URL enforcement is real (discriminated union, all charts annotated) with all 43 sources
non-empty; vendor strengths genuinely conceded; every numeric product claim traced to its
constant; the provincial/municipal receipting and US-gift limits correctly stated; the alert-rule
values valid and name-keyed safely.

---

## What remains open after this review

**Operator actions (cannot be done from the repository):**
everything in REVIEW6's tables A/B, unchanged — live Stripe keys/prices/webhooks/Tax
origin/dunning/portal, Postmark and SendGrid go-live, the platform sending domain, the
boundary-map upload (`npm run boundary-catalog -- build` / `-- upload` — now also a prerequisite
for four marketing pages, per F2), `WEBAUTHN_RP_ID`, the Twilio end-to-end test, OAuth
publishing, `OPS_ALERT_SMS_NUMBER`, Postgres scale-up, the test-data wipe, the TRUST_PROXY
measurement — plus, new from this review, the orphaned-webtest portal check (F3).

**Deliberately left, with reasons recorded above:** C6, C7, D6, D7, E6, E7, F4, F5; REVIEW6's
T2-16b/c, T2-18, the T2-1 request-path parse, and the Container-App bicep declaration.

**Known small caveats in the fixes themselves:** the breach lists' per-page recompute survives
(bounded at 2,000); the boundary re-scan on an unloadable layer survives (now alerted, B4); the
unpaid-demo SMS skip also silences the phone-verification code inside the demo (the volunteer
gate is partially un-testable in demo — accepted as the price of C1); Microsoft Graph
attachments above ~3 MB still need an upload session (pre-existing, noted by sub-review B).

**Not reviewed by any of the seven reviews:** test-file internals (deliberate), the Nx/tooling
configuration, and live infrastructure state (Container App env values, Stripe dashboard,
Cloudflare/Azure portal state).
